# Build-time defaults, committed on purpose. Secrets belong in .env.local,
# which git ignores.

# Keep the webpack runtime as its own file instead of an inline <script> in
# index.html. Required for the strict `script-src 'self'` CSP in index.html —
# without it the inline runtime is blocked and the app never boots.
INLINE_RUNTIME_CHUNK=false
